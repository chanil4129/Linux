#include <stdio.h>
#include <string.h>

int find_dfs(int index){
	return 1;
}

void aa(char *a){
}
void find_command(char *file){
	aa(file);
}
int main(void){
	char *file;
	int index=0;
	index=find_dfs(index);
	printf("%d\n",index);
	find_command(file);

	return 0;
}

